{
  "name": "fabian-portfolio-website",
  "version": "1.0.0",
  "description": "Professional portfolio website for Fabian Fernandes - Cloudflare Workers compatible",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "mkdir -p public && cp -r ../portfolio-site/* public/"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}